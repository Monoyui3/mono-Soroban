version: 2
workflows:
  build-ios:
    triggers:
      - event: push
        branch: main
    environment:
      xcode: latest
    scripts:
      - name: Install dependencies
        script: flutter pub get
      - name: Build iOS app
        script: flutter build ipa --release
    artifacts:
      - build/ios/ipa/*.ipa
